services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - asya_data:/app/data
      - asya_tmp:/app/tmp

volumes:
  asya_data:
  asya_tmp:
