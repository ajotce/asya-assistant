# OPENCODE.md

> Этот файл — тонкая обёртка для opencode (https://opencode.ai). Все основные правила работы — в `AGENTS.md`. Здесь только opencode-специфика.

## Главное

opencode по умолчанию читает `AGENTS.md` в корне проекта — это и есть основной источник правил. Прочитай его перед началом работы.

После `AGENTS.md` прочитай `docs/roadmap.md` и файлы `docs/acceptance/v0.X.md` для текущей фазы.

Текущая фаза: `v0.5 — Extended integrations and documents`.
Текущая базовая ветка фазы: `0.5-extended`.
Параллельная работа: только через feature-ветки от `0.5-extended`, без прямых коммитов в `main`.

## opencode-специфика

### Использование разных моделей

opencode умеет работать с разными моделями (Claude, GPT-4, локальные через Ollama и т.д.). Для этого проекта рекомендуется:

- **Архитектурные задачи, обзор кода, написание тестов** — Claude или GPT-4 уровень.
- **Простые правки, форматирование, рутинные миграции** — модели уровня Sonnet/4o-mini.
- **Никогда** не использовать локальные модели для задач с реальными секретами или критичной логикой авторизации/шифрования — слишком высок риск ошибок.

### Slash-команды и agents

Если используешь кастомные slash-команды или sub-agents в opencode — храни их конфиги в `.opencode/` и коммить вместе с проектом (но без секретов).

### Подтверждения перед изменениями

opencode может работать в режиме автономного выполнения. Для этого проекта **всегда** требуй подтверждения перед:

- Применением миграций (`alembic upgrade`/`downgrade`)
- Удалением файлов или записей в БД
- Изменением `.env`-файлов и секретов
- Push в удалённый репозиторий

## Ссылки

- `AGENTS.md` — основные правила
- `docs/roadmap.md` — план развития
- `docs/acceptance/v0.5.md` — критерии приёмки текущей фазы
