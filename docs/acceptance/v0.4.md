# Acceptance v0.4

Статус: draft/finalization.

## 1. Integrations baseline

- [x] Есть user-scoped `integration_connections`.
- [x] OAuth state + PKCE foundation присутствует.

## 2. Alpha/Beta access flow

- [x] Публичный submit access request: `POST /api/access-requests`.
- [x] Admin approve/reject endpoints есть.
- [x] Approve генерирует one-time setup link.
- [x] Setup password endpoint: `POST /api/auth/setup-password`.
- [x] Решение зафиксировано в ADR как НЕ passwordless login.
- [x] Email transport abstraction: mock/smtp.

## 3. Chat action routing + confirmations

- [x] Есть `ActionRouter`, связанный с `ChatService`.
- [x] Зарегистрированы tools: calendar list/create, todoist list/create, linear update, gmail search/draft.
- [x] Работает pending -> confirm -> execute (`/tool ...`, `/confirm <id>`).

## 4. Activity log safety

- [x] Integration action события пишутся в `ActivityLog`.
- [x] Observer события пишутся в `ActivityLog`.
- [x] В `meta` пишутся только safe metadata, без секретов/тел писем/аудио/контента файлов.

## 5. Deployment readiness

- [x] Выбран reverse proxy: Caddy.
- [x] Добавлен production compose-файл.
- [x] `.env.example` расширен под public URL/OAuth redirects/SMTP/Telegram webhook.
- [x] Обновлён `docs/deployment.md` с security checklist.

## 6. Virtual final check

- [ ] backend tests: blocked локальным Python 3.9 (проект ожидает 3.11+ синтаксис аннотаций).
- [ ] frontend lint/typecheck/tests: частично (см. отчёт в финальном ответе).
- [ ] observer + confirmations smoke: ручная виртуальная проверка по коду выполнена.
